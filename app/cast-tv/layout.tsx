export const metadata = {
  title: "CAST-TV",
  description: "Fitdog CAST-TV digital signage slideshow"
};

export default function CastTvLayout({ children }: { children: React.ReactNode }) {
  return children;
}
